# Bitcoin AI Analysis Agent

## Project Goal

An AI-assisted system that checks on Bitcoin on a regular schedule and produces one of three signals — **BUY, HOLD, or SELL** — along with a **confidence level** and a **plain-language explanation**.

It looks at price/history, trends, volume, momentum (technical indicators), chart patterns, and relevant news. Each of these gets its own score, and the scores combine into one overall score plus a confidence level.

The system leans on solid, deterministic math (indicators, formulas) for anything math can reliably do, and uses AI specifically where AI adds real value (mainly reading/interpreting news, and writing explanations) — not as a vague "will Bitcoin go up?" question.

**This is analysis only.** It does not place real trades — not in this phase, and not any time soon.

## Current Phase: Phase 1

Scope: build the **backend**, the **AI agent**, and the **analysis engine** only. No frontend yet — that comes later, built separately in Lovable.

Once running, Phase 1 should operate on its own on a schedule (roughly hourly), without needing the user's phone or computer to be on. Each automatic run should roughly:

1. Collect fresh Bitcoin market data
2. Calculate relevant indicators and features
3. Collect relevant news
4. Analyze all of it
5. Run the scoring system
6. Produce BUY/HOLD/SELL, a confidence level, and an explanation
7. Store the full analysis in a database/log
8. (Later) compare each prediction with what actually happened afterward

## Important Principles

These rules govern every decision in this project. They should not be broken without stopping to discuss it first.

1. **Build the intelligence before the UI** — get the analysis right before worrying about how it looks.
2. **Prioritize data quality** — good data matters more than clever code.
3. **Avoid data leakage** — when testing against old data, the system must never be given information it wouldn't have actually known at that point in time.
4. **Avoid overfitting** — don't tune the system to match old data perfectly; that makes it good at explaining the past and bad at predicting the future.
5. **Log everything important about every prediction** — full inputs, scores, and outputs, every single run.
6. **Make confidence measurable and calibratable, not guessed** — confidence has to be checked against real outcomes over time, never just a number the AI states on its own.
7. **Test every meaningful change against historical data** before trusting it.
8. **Don't assume a signal is good just because the AI's explanation for it sounds convincing** — a persuasive-sounding explanation is not evidence that the call was right.
9. **Keep the architecture modular** — the AI model, data providers, technical indicators, scoring system, news sources, database, and frontend must each be swappable without rebuilding everything else.
10. **No real trading** — this project stays analysis-only for now, and for the foreseeable future. Do not implement real trading in this phase or any time soon.

## Architecture Decisions Log

_Decisions get added here once they're actually made and approved together with the user — not before._

Phase 1 architecture **approved 2026-09-19**. Full reasoning lives in the approved plan (`C:\Users\aarsn\.claude\plans\jolly-leaping-lampson.md`); this is the durable summary for future sessions:

- **Language**: Python — best-fit ecosystem for data handling, indicators, and AI glue code; no performance need to justify anything else.
- **Scheduling**: GitHub Actions scheduled workflow (hourly, off-peak minute) + a free heartbeat/dead-man's-switch alert (e.g. healthchecks.io) to catch silently-skipped runs. Repo recommended public (unlimited free Actions minutes, and there's nothing proprietary to protect); user may choose private instead — their call.
- **Market data**: Binance public API (primary) + CoinGecko (automatic backup). The plan originally had these the other way round; swapped during the build because CoinGecko's free tier gives no true per-hour volume, and volume is a required analysis category. Binance is reached via its public data mirror (`data-api.binance.vision`) first, because the main API refuses US addresses and GitHub's job runners are US-based. Price data is critical enough to need a fallback: missing it blocks the whole hourly run.
- **News**: Free RSS feeds from reputable outlets (e.g. CoinDesk, Cointelegraph). CryptoPanic's API turned out to be paid-only now (checked, not assumed). Headline + short snippet only, no full-article scraping; de-duplicate stories that appear on multiple feeds.
- **Database**: Supabase (hosted Postgres) — reachable from a cloud job, comfortably covers Phase 1 data volume on the free tier, and is the natural fit for the future Lovable frontend. Watch item: free projects auto-pause after 7 days idle; hourly writes should prevent this, but verify in practice during the first couple of weeks.
- **AI**: Claude, used in exactly two narrow, structured calls per run — never a free-roaming autonomous agent. (1) News headlines → a structured sentiment/relevance score (Haiku 4.5). (2) Final plain-English explanation, generated *after* the decision is already made by deterministic scoring — it narrates the result, it never judges or restates confidence. Estimated cost: well under $10/month at hourly cadence.
- **Confidence**: calculated from (a) agreement between category scores and (b) data completeness — never AI-guessed. Individual components are logged, not just the final blended number. Labeled explicitly as "a consistency/completeness estimate, not yet a calibrated probability" until enough real outcome history exists to actually calibrate against.
- **Indicators library**: pandas-ta-classic (the actively maintained community fork; the original pandas-ta looks at risk of going unmaintained).
- **Chart patterns**: simple deterministic rules only in Phase 1 (e.g. moving-average crossovers, higher-highs/higher-lows structure) — no image-based pattern recognition yet.
- **Backtests are technical-only and never touch the predictions table.** RSS has no archive, so past news is unknowable; backtests run with news absent (weight 0) and are labeled as such — not directly comparable to live runs. Results go to CSV files under `backtests/` (git-ignored), keeping live data pure. The no-lookahead rule is enforced by construction (each hour sees only `bars[:i+1]`) and checked by `tests/test_point_in_time.py`.
- **Outcomes record raw prices and returns only.** Whether a signal was "right" is decided at analysis time (which horizon, what threshold, vs. buy-and-hold), never by the tracker. Horizons: 1h, 24h, 168h.
- **Module map & build order**: see the full plan file — 13 build steps from project skeleton through to scheduled go-live, each module with a narrow, swappable interface (settings, market data, indicators, patterns, news, scoring, AI, decision/confidence, database, orchestrator, backtest runner, outcome tracker).

## Status

- Phase 1 plan approved 2026-09-19. Implementation started same day.
- Built and tested against real live data: market data (Binance primary, CoinGecko backup), indicators, chart patterns, news (RSS), scoring, and the decision/confidence step. Verified end-to-end with `run.py`. Committed locally to git (not yet pushed to GitHub).
- Implementation refinement vs. the original plan text: **Binance is the primary price source, CoinGecko is the backup** (swapped from the plan's initial framing). Reason found while building: CoinGecko's free OHLC endpoint doesn't actually include volume data, and volume is one of the required analysis categories. Binance's public endpoint gives true open/high/low/close/volume directly. Both sources are still used, exactly as planned — just swapped which one leads.
- AI module and database are now built and working end-to-end. First real prediction saved to Supabase 2026-09-19. Anthropic API key and Supabase connection both live in `.env` (git-ignored).
- Lesson worth keeping (rule 8 in action): the first AI-written explanation confidently described "agreement" as covering all five categories, when it actually covers only the independent ones. The prompt in `agent/ai/explainer.py` now states precisely what each confidence component measures and passes each category's independence flag. A convincing explanation was wrong about our own math — worth re-checking whenever the explainer prompt or confidence formula changes.
- Outcome tracker, backtest runner (with point-in-time guard test), and the GitHub Actions hourly workflow (`.github/workflows/hourly.yml`, runs at :17 past each hour) are built. First real outcome row recorded 2026-09-19.
- First 30-day backtest of scoring v0.1.0 (2026-08-20 → 2026-09-19, technical-only): **no measurable edge.** BUY hours averaged +0.33% over 24h vs. +0.33% for all hours; SELL hours averaged +0.52%. Expected for a first formula. Do not tune weights against this window (rule 4) — collect live outcomes first, and keep a held-out period untouched when tuning does begin.
- **Live since 2026-09-19 12:20 UTC.** Code is on GitHub at `ja-284/Bitcoin-AI-Agent` (public, branch `main`); secrets ANTHROPIC_API_KEY and DATABASE_URL are set; the first manual run from GitHub's servers succeeded (42s) and wrote prediction #2. The schedule fires at :17 past each hour.
- Phase 1 scope is complete. The signal itself is NOT good yet (see backtest above) — "live" means the measuring instrument is switched on and collecting real outcomes, not that the analysis is finished. Improving the scoring is the next phase, done against accumulated live outcomes + a held-out period, never against a single backtest window.
- Watch items for the first weeks: (1) confirm scheduled runs actually fire (Actions tab; GitHub can delay or skip) — optional heartbeat via healthchecks.io + HEARTBEAT_URL secret not yet set up; (2) confirm Supabase doesn't auto-pause; (3) GitHub disables scheduled workflows on public repos after 60 days with no commits — any commit resets the clock, and GitHub emails a warning first.
- An older branch `claude/sleepy-fermat-wmrosq` on GitHub (2026-09-18, a first CLAUDE.md from a web session) is superseded and can be deleted.
- **E001 (2026-09-19): scoring 0.1.0 has no measurable predictive value.** 68,619 replayed hours, 2017 → mid-2025, five horizons, pre-registered criterion not met anywhere: no edge beyond buy-and-hold drift, direction accuracy 47–50% on acted hours (below always-UP), sign flips year to year, and the confidence heuristic is uninformative (stated 0.7–0.9, observed ≈ 0.48 in every bucket). **Do not tune 0.1.0's weights or thresholds.** Details: `research/results/E001/summary.md`.
- **Scheduling (2026-09-19/20):** GitHub's own cron ran 2 of 8 slots on day one; with four slots/hour it covered ~9 of 11 hours overnight. **Fixed 2026-09-20 08:12 UTC:** Supabase `pg_cron` + `pg_net` posts to GitHub's `workflow_dispatch` API at :12 each hour (token in Supabase Vault, `docs/ops/external_trigger.md`); first dispatched run started within 1 second and succeeded. GitHub's own four slots/hour remain as backup. If runs stop, check `cron.job_run_details` and `net._http_response` in Supabase first (a 401 there means the token is wrong/expired). The GitHub fine-grained token `supabase-dispatch` **expires 2027-09-20 — regenerate and re-store it before then.** Healthchecks.io heartbeat still not set up.
- **2026-09-19 evening — correctness phase (pipeline 0.2.0).** Inspection report found and the fixes addressed: GitHub skipped 6 of the first 7 hourly slots (now two slots/hour + self-check + watchdog); news leaked past the reference candle's close (now an explicit `cutoff_at`, news limited to it, undated items excluded); backtest outcomes were found by row count not timestamp (fixed; gaps → unavailable); backtest used a growing window unlike live (fixed: exactly 250 bars); fallback volume was scored on a rolling-24h figure (now unavailable); no candle validation (added; gaps flagged, never filled); outcome tracker guards and `unavailable` status. Full list in `CHANGELOG.md`. 43 tests. Predictions 1–4 are pipeline 0.1.0 (news not cutoff-limited).

**2026-09-22 (evening) — E018, the first of the master plan's sections 14-19 (signal discovery / ablation / redundancy / "how many signals?").** Applied to the only model that ever passed a pre-registered test, the 1h move-size model: 48 variants, 27 folds, 54,472 out-of-sample hours, every variant scored on identical rows (`agent/research/feature_count.py`, `research/results/E018/`). **Three findings. (1) One of the nine inputs is arithmetic, not information:** after the log transforms `vol_ratio_24_168` = `rv_24` − `rv_168` exactly (1.0e-15 over 51,053 hours), so `move_size_1h_v1` has **eight** independent inputs; dropping the redundant one moves validation Brier by +0.0045% relative. **(2) The model is over-featured:** six features reach 99.4% of the best validation skill, four reach 95.6%; ablation costs are volatility −61%, trade intensity −18%, calendar −6% of skill; coefficient signs are stable. **(3) My pre-registered decision rule was the wrong instrument** — it used a *paired* bootstrap SE, which shrinks as fast as the difference it measures, so it answered k = 9; the classical unpaired one-SE rule answers k = 4. The pre-registered answer is recorded as primary and the defect is recorded with it (never silently re-decided). **Nothing that runs was changed** — the frozen artefact keeps all nine inputs and keeps running in the shadow record, because the smaller sets are not better (only indistinguishable in practice and slightly worse in point estimate) and swapping would reset the only prospective evidence this project will ever have. Timing note for the user: if the model is ever to be simplified, the cheapest moment is while the prospective record is short. Incidental but valuable: the nine-feature run reproduces E013 to the digit from an independently written evaluation path. 234 tests. **E019 (same evening, master plan sections 24-25): the move-size model was measured for the first time against a rule that needs no fitting** — "what share of recent hours moved more than 0.25%?". Seven candidates on identical validation rows. The honest non-fitted baseline is strong (a 24h EWMA reaches 40% of the model's skill with zero fitting), but the model clears both pre-registered bars comfortably: skill +0.098 vs +0.040, **2.47x**, paired Brier difference +0.01459 [+0.01261, +0.01652]; giving the simple rules the incumbent's own Platt calibration changes nothing (2.33x). **The model's complexity is now defended rather than assumed.** Standing consequence: every future move-size result must be reported against the EWMA rule too — E012's base-rate comparison set the bar far too low. 241 tests. **E020 (sections 24-25): a fancier model does not help.** Three families, settings fixed in advance, run once each on identical rows. Gradient-boosted trees reach 1.04x the logistic's validation skill — paired interval excludes zero (+0.00093 [+0.00006, +0.00178]) but the practical bar of 1.10x fails, so the recorded verdict is *statistically better, practically equivalent*. A logistic with 21 squared/product terms reaches 1.01x and **fails calibration** (worst bucket 0.054), which disqualifies it regardless of Brier. **The logistic stands**, and "should we use a fancier model?" is now a pre-registered NO rather than an assumption. 247 tests. **Backend contract v1 built (sections 39/52):** `agent/api/state.py` + `docs/api/contract_v1.md` — read-only, not yet wired to anything. It is the single place that decides what the backend SAYS about itself, and it enforces one rule: a number that is not a validated probability must not be able to look like one. Every quantity is `{value, kind, is_probability, meaning}`; the signal always carries "no demonstrated predictive value"; an unavailable category is distinguishable from a neutral one; a test walks the whole structure and asserts the only probability-flagged field is the calibrated move-size one. Staleness is now defined once (`agent/healthcheck.check`) and called by the self-check, the watchdog and the contract. **Transport deliberately not decided** — recommendation is to have the hourly job write the state into a one-row table (see the doc). 266 tests. **Readiness gate revision 2 (same evening):** the master plan's 22 categories scored individually in `docs/research/readiness_gate.md` — **19 PASS, 3 PARTIAL, 0 FAIL, 0 UNKNOWN**. The three PARTIALs are the heartbeat (user, 5 min), the least-privilege DB role (user, 10 min) and prospective monitoring (nobody's — it needs 500 elapsed hours). Feature redundancy moved UNKNOWN → PASS (E018) and model behaviour → PASS (E019/E020). **"Backend finished" is NOT claimed**; the honest statement is *ready to be built against, not finished*, and the signal still has no demonstrated predictive value. **E019's reference is now wired into the weekly report (pre-committed):** every future judgement of the live shadow record is scored against the no-fitting 24h EWMA rule on the same hours, using the same function the experiment used, so the monitoring and research references cannot drift apart. **First reading — a watch item, not a finding:** over the first 18 prospective hours the model is BEHIND the reference (Brier 0.2574 vs 0.2258) where development said it should be ~2.5× more skilful; large moves occurred in 33% of those hours vs ~47% in development and the model kept stating 0.47–0.55. **n = 18; intervals need 192.** Recorded in `research/ROADMAP.md` so it can neither be forgotten if it persists nor treated as a finding if it vanishes. Read at the 500-hour checkpoint. Nothing changed. 270 tests. **E021 (section 20; pays the 2026-09-19 debt "threshold fixed-% vs volatility-scaled to be tested, not assumed"): the fixed target stands.** Fixed 0.25% vs 1.0× the median of the last 720h of completed returns, identical rows/folds/features. Scaled wins stability enormously (yearly positive rate 0.476–0.508 vs the fixed target's **0.336–0.676**) and calibration (ECE 0.012), but fails predictability (skill 0.074 vs 0.098 = 0.75×, bar 0.90×) and ties on ranking (ρ 0.338 vs 0.340, failed by 0.002). **The real finding is the decomposition:** the E019 reference scores +0.0398 against the fixed target and **+0.0003 against the scaled one**, while the model scores +0.0983 and +0.0736 — so most of the model's apparent skill is knowing the volatility *level* (free from an EWMA) and roughly +0.074 is its own *within-regime timing*. Describe the deliverable that way from now on. Also gives the 18-hour watch item a mechanism (a fixed bar changes meaning when the market calms) — a mechanism, not evidence. 277 tests. **E022 (exploratory, follows E021): the model's own contribution may be TRADE INTENSITY, not volatility.** E018's machinery re-run against the volatility-scaled target (level removed from the question): the volatility group's ablation cost collapses **61% → 14%**, trade intensity becomes the largest at 23%, the first pick flips from `tr_mean_14_rel` to **`trades_rel_168h`** (88% of the nine-feature skill from one input), and the absolute-volatility coefficients REVERSE sign. All 3 pre-registered predictions held. **Nothing adopted.** **New binding caveat — VALIDATION WEAR:** 20 experiments had examined the validation period before E022, so validation results are now hypothesis-generating, not confirmatory; the sealed holdout is the only clean arbiter left; and any future claim that something works must state how many experiments had already seen its data. 286 tests. **Observability review done (`docs/ops/observability.md`):** every failure handler in the live path audited against "a failure must never be indistinguishable from a real observation". Found one gap — a *partial* news failure (one feed down) still produced a number and was invisible; now surfaced in the contract (`sources_used`/`sources_total`), counted in the weekly report, and covered by tests. Never occurred in 69 live runs, which is why it needed a test not a watch. Deliberately NOT done: weighting news by source coverage (would change live scoring for an effect that has never happened). **The one real remaining gap is not code:** every alarm lives inside GitHub, so GitHub going quiet looks like success — the heartbeat step is written and inert, waiting on the user's secret. 290 tests. **Schema-mismatch guard added** (§30's last untested failure): `assert_schema_current()` runs before any write — schema 3 is what added the CHECK constraints and append-only triggers, so an older database would accept rows the code believes impossible. Verified against the live database before committing (0.5s, 3 = 3). It caused an immediate regression worth remembering: five orchestrator tests started opening real DB connections and still passed locally because `.env` works — **CI now runs the suite with a deliberately unreachable `DATABASE_URL`** so that can never pass unnoticed again. 292 tests. **Watch-item update (same evening): it largely dissolved on a larger sample.** The E019 reference comparison was extended to the *paper* record (all 82 live hours, not just the 18 the shadow covers). There the model is **ahead**: Brier 0.2058 vs 0.2192 (−0.0134 in its favour), skill +5.0% vs −1.2%, in a period where large moves happened in 32% of hours. The 18-hour reading was noise, which is exactly what the watch item was written to be able to show. Qualifications recorded: the 18 are a subset of the 82 (more data, not independent confirmation) and the paper record is after-the-fact (fine for comparing two models on identical rows, not prospective evidence). **Still read at the 500-hour checkpoint.** **PAUSED 2026-09-22 ~20:15 UTC.** Tree clean, `main` = `origin/main` at `9c60010`, 296 tests pass with no database reachable, holdout sealed, live system healthy. The schema guard (`2cef624`) is **verified in production**: the 19:00 UTC prediction was written at 20:12:38 by `0732a10`, with its shadow row and no errors — nothing from today is left unobserved live. **First thing on resume:** wire `python -m agent.api.publish` into the hourly workflow (exact YAML in `docs/api/contract_v1.md`; left unadded on purpose so it can be watched on the next run). Full resume note in `research/ROADMAP.md` → "Paused 2026-09-22".

**2026-09-23 — security finding, schema 4, migration entry point (under the master-plan addendum: exit gates, hard vs soft gates, validation wear enforced, security first).** Supabase's advisor flagged RLS off on a table; investigation found **all six tables** readable AND writable through Supabase's public API by anyone holding the project's anon key (RLS off, full privileges for `anon`/`authenticated` via Supabase's default privileges). **No sign in the data of any outside write** (91/91 hours, none future-dated, every commit stamp known); whether anything was *read* is **UNKNOWN** until the user checks Supabase → Logs → API. Neither the project hostname nor the anon key appears anywhere in the repo or its history. **Closed** with two independent layers in every schema file (RLS on, no policies; API roles' privileges revoked), proven by behaviour (48/48 anonymous probes refused) and per layer on real Postgres; the watchdog runs `python -m agent.database.security` every 3h. **Schema version 4**, applied database-first then code, after the schema guard was changed to require *at least* the code's version (exact equality made every migration a failure window; migrations are additive). **My 2026-09-21 security audit had missed this** — the readiness gate carries a dated correction: Security was really FAIL until today. Found while fixing it: the shadow step re-applied its schema DDL every hour and the documented fresh-database command built only one of three schema files — now `python -m agent.migrate` is the only way to change the schema and the hourly job runs no DDL. The least-privilege role's SQL was wrong twice (no policies; then an UPDATE policy without WITH CHECK that would have refused every shadow grading) — it now lives in `docs/ops/least_privilege_role.sql`, proven by an integration test that runs every job write path as a throwaway restricted role and fails on the buggy policy. Readiness: **RELEASE CANDIDATE, not FINAL** — every hard gate PASS; open: heartbeat (user), least-privilege role (user, now ready), API-log check (user, UNKNOWN), prospective monitoring (calendar). 335 unit tests with no database reachable, 14 integration tests.

**2026-09-23 (afternoon) — hardening close-out.** The migration entry point is **verified in a scheduled run** (GitHub's 15:09 UTC slot: prediction + shadow row, every step green, 0 shadow errors). **E024** measured the live checkpoints' own error rates before the data exists (E013's 5,000-hour bucket rule fails a perfectly calibrated forecaster 8% of the time; the 18-hour watch item was a one-in-four event); no rule changed. **E023** registered this week's validation-generated findings (E018–E022) as secondary hypotheses for the sealed holdout, made testable and dry-run on validation; **E014 amended while sealed** (it still named scoring 0.1.0; live is 0.2.0) and the script refuses an unregistered scoring version before loading a candle. Holdout readiness: **NOT READY** (prospective monitoring ~37 of 500 hours); stays sealed. **Mutation testing (`tools/guard_mutations.py`): 28 of 28 guards caught in one pass, both controls held**; round two had found two real gaps (the historical replay behind E001/E017 could read a candle from the future without any test noticing; E023 could train its compared models on different rows), both closed with tests proven to kill them. **`python -m agent.api.publish` now runs every hour** (last real step, cannot fail the job) — verified at the 16:12 UTC run together with the cost record (every step green, snapshot describing the 15:00 hour, `ai_usage` present). **The AI cost is measured, not estimated:** every run records the API's own token counts in `run_meta.ai_usage` (missing figures = `usage_unavailable`, never zero); the weekly report prices them against a dated list-price table in `weekly_report.py` (Haiku 4.5 $1/$5, Sonnet 5 $2/$10 per million tokens, 2026-09-23). First real reading at 56 headlines: **$0.0164 a run ≈ $11.80 a month** (the old $0.02/$15 estimate was a little high); the news-cost decision stays the user's. Operational note: GitHub's unauthenticated API allows 60 requests an hour — when waiting on a run, poll the database, not GitHub. 368 unit tests.

**2026-09-23 (evening) — security review completed on the user's instruction.** Supabase API logs checked by the user: nothing in the retained window (Free plan keeps 1 day, so ≈ the last 19 h of the exposure); **2026-09-19 → 09-22 ~18:00 is UNKNOWN permanently** — never write "not read". **Root cause closed:** Supabase's default privileges granted every NEW object in `public` to `anon`/`authenticated` (24 standing grants); `agent/database/schema.sql` removes the rule for the owner role, applied live (24 → 0), proven live in a rolled-back transaction and with a control on real Postgres. This reversed the morning's recorded "default privileges left alone" decision, with the reason recorded. **Detector** now also fails on API-readable views (views ignore RLS), functions callable at `/rest/v1/rpc`, and default grants; Supabase-owned rights it cannot revoke are NOTES. **Cannot be closed by this project (tried; Postgres refused):** `anon` holds full rights on `net.http_request_queue` (the dispatch request, GitHub token included, sits there for seconds) and EXECUTE on `net.http_post` — reachable only if `net` is in Dashboard → Project Settings → API → *Exposed schemas* (default no). **That setting is UNKNOWN until the user checks it — the single remaining control over `net`.** No secret in stored query texts (the one token-shaped match is the setup script's placeholder, proven ≠ the Vault token). Heartbeat: the user tried healthchecks.io and it did not work — deferred by the user; the `HEARTBEAT_URL` GitHub secret is NOT set (step shows *skipped*). A `HEARTBEAT_URL` line exists in the local `.env` (value never read). 375 unit tests, 18 integration tests, mutation testing 31 of 31. **Later the same evening:** the least-privilege switch is now *checkable* — `python -m agent.database.role_check` compares the live `bitcoin_agent` role with `docs/ops/least_privilege_role.sql` (expectations parsed from the file itself; prints `NOT READY` until the user creates the role), and every prediction records the role that wrote it (`run_meta.db_role`, stamped by the INSERT itself via `current_user`). Heartbeat steps hardened (`continue-on-error`; a failed run sends healthchecks.io's `/fail`), inert until the secret exists. News cost **decided by the user: keep at ~$12/month, monitored** — the weekly report flags a projection above $15. Two report counting fixes: shadow coverage uses the due-hours rule; the news-evaluation counter counts only hours with a *usable* news score (the 17 failed-scoring hours no longer count). The watchdog is GitHub-scheduled and ran only 3 of 8 slots in a day (its checks passed, including the first security check from GitHub). Prospective shadow hours: 42 of 500 (2026-09-23 19:55 UTC). 397 unit tests, 19 integration tests, mutation testing 34 of 34. **Live checkpoints prepared** (`agent/research/live_checkpoint.py`; fixed-prefix rule, E024's pass rules, regime terciles frozen from development data — three clarifications in `research/LIVE_EVALUATION.md` dated before any checkpoint data). **PAUSED 2026-09-23 ~20:15 UTC** — `main` = `origin/main`, tree clean, 415 unit tests, mutation testing 36 of 36, holdout sealed; `db_role` and `ai_usage` verified live on the 19:00 prediction. **First thing on resume:** confirm the hourly runs after the pause are green with both heartbeat steps *skipped* (the heartbeat workflow change `d71c09f` was pushed at the pause and not yet seen in a scheduled run — if a run failed to start, revert it first). Full resume note: `research/ROADMAP.md` → "Paused 2026-09-23".

**2026-09-24 — resumed; the pause verified; E025.** Since the pause: 21/21 hours, 38/38 GitHub runs green, both heartbeat steps *skipped* as designed (heartbeat workflow change verified), 3/3 watchdog runs green with the widened security check, integration + drift 19/19, parity 114/114, cost ≈ $13.13/30 days (rising with headline volume; flag at $15). **User-owned items checked, none done:** heartbeat secret not set; `bitcoin_agent` role absent; Exposed schemas UNKNOWN. **E025 (planning, read the news score only — never an outcome):** at 500 usable hours the news test can only detect ρ ≥ 0.125 at 1h (0.27 at 6h, 0.42 at 24h); ρ = 0.05 at 1h needs ~3,140 hours. **Registered design for roadmap 8.6:** 1h primary; 500 h = first look, no verdict; verdict from 3,140 h; 6h/24h descriptive. **Side finding:** the 48h block-bootstrap interval gives ~9% false positives at 500 hours (nominal from ~2,000); intervals below 2,000 hours are now labelled optimistic in the weekly report — no checkpoint rule changed. I first told the user the 10% "did not hold up" on the strength of one 600-record check; pooling both independent seeds says ~9% — corrected in the same session. 424 unit tests, mutation testing 38 of 38. **PAUSED 2026-09-24 ~17:45 UTC** — tree clean, pushed, holdout sealed, live healthy. **First thing on resume: the *Exposed schemas* check with the user** (Supabase → Project Settings → API; expected only `public`, `graphql_public`), then the usual overnight verification. Resume note: `research/ROADMAP.md` → "Paused 2026-09-24". **Later 2026-09-24:** *Exposed schemas* confirmed by the user (`public`, `graphql_public`) — "critical security" PASS without condition. Least-privilege setup reduced to one command the user runs themselves, `python -m agent.database.setup_role` (generates the password; sends only a SCRAM verifier, because `CREATE ROLE ... PASSWORD 'plain'` would sit in `pg_stat_statements`; tests the login; puts the new connection string on the clipboard) plus `python -m agent.database.try_connection` (hidden prompt). **2026-09-25 status check:** 21/21 hours, 40/40 runs, 4/4 watchdog, 0 shadow errors, parity 135/135, 84 of 500 prospective hours (checkpoint ≈ 2026-10-12); shadow/live separation now a tested guard; the AI-cost rise is the weekday/weekend news cycle (weekends ~16–20 headlines, weekdays 55–72), so weekday-only projections over-state a month (≈ $12 expected) — the report now says "partial week". Least-privilege role and heartbeat still OPEN (user). 441 unit tests, 42 mutation guards. **2026-09-25 evening — both done by the user.** Least-privilege: `setup_role` run by the user (from `C:\Users\aarsn\bitcoin-agent` — note a second, non-project folder `C:\Users\aarsn\Documents\bitcoin-ai-agent` exists and has no `.venv`), `role_check` OK, `DATABASE_URL` secret switched; **the 19:12 UTC run wrote as `bitcoin_agent`** (prediction, outcome insert, shadow insert, shadow grading, publish — all green). The local `.env` keeps the owner role (migrate, weekly report, integration tests). **Readiness row 17 Security: PASS.** Heartbeat: healthchecks.io check (1 h period, 30 min grace), secret `heartbeat_url` (case-insensitive = `HEARTBEAT_URL`); the 19:12 run's ping step ran (*success*) — row 16 moves to PASS when the ping is seen on healthchecks.io (the step is `continue-on-error`, so GitHub's green does not prove delivery). Still to observe: the first watchdog run as `bitcoin_agent`. **Heartbeat VERIFIED by the user** (green on healthchecks.io, 19:13 UTC ping received) — readiness now **21 PASS, 1 PARTIAL** (row 21, prospective monitoring, by the calendar). The drift check now builds its copy from the schema files **plus** `least_privilege_role.sql` (the live `bitcoin_agent` policies would otherwise read as drift) and proves a hand-made policy still shows; 22 integration tests. Runtime re-measured: job 74–79 s (median 87 s incl. GitHub queue), under a tenth of the budget. **From 2026-09-25 the backend is in the prospective-validation stage (user instruction): no new features, models, indicators or architecture for activity's sake; monitor, audit, and evaluate the 500 / 2,000 / 5,000-hour checkpoints exactly as registered; a strong checkpoint triggers a strict final readiness audit, never an automatic "finished".**

**2026-09-26 — ACTIVE PLAN: `research/INTERIM_PLAN_PRE_500H.md` (the user's pre-500h interim execution plan). Every session starts there and at `docs/ops/STATUS.md` (+ `status.json`), the operational status for unattended running.** Predictive path frozen; prospective outcomes are observations only; no frontend, no ML, no busywork; priority A live health → B security → C reliability → D observability → E performance (evidence only) → F research integrity. Done today: 14/14 overnight hours all written by `bitcoin_agent`, 25/25 runs, heartbeat ping on every run, 2 watchdog runs green after the switch; the watchdog now also runs `python -m agent.database.role_check --connected` (fails unless the jobs connect as `bitcoin_agent` with exactly its proven rights — proven to fail as the owner); storage 16.6 MB of 500 MB (~15 MB/month); staleness alarm proven to fire. 103 of 500 prospective hours; checkpoint ≈ 2026-10-12. 444 unit tests, 43 mutation guards. **Later 2026-09-26:** the watchdog **verified under `bitcoin_agent`** (11:42 UTC run, every step green including the new role check); full mutation run 43/43; the chain candles → inputs → probability verified for all 106 stored shadow hours (weekly report checks inputs vs research; every checkpoint states probability reproducibility); first drift flag (outcomes: a calmer live market, 34% vs 47.5% large-move hours; pipeline ruled out) → **E026** (pre-registered, development data): the model family was calibrated in calm markets historically, so a *persistent* live calm-market over-prediction would be a genuine deviation — watch item for the registered checkpoints, never tuned. 455 unit tests, 44 mutation guards. **Afternoon (the plan's own audit, `research/INTERIM_PLAN_AUDIT.md`):** B1 actions pinned to SHAs, B2 dependency audit (0 known vulnerabilities), K1 checkpoint command proven end to end. **E027:** good overall calibration hides offsetting intraday miscalibration (nights over-stated +0.047/+0.022, US session 12–18 UTC under-stated −0.022/−0.057) — the 2,000 h slices are read against these offsets, not zero. **E028 (research-only): a second calendar harmonic does NOT remove it** (worst block 0.057 → 0.064 on validation) — negative, nothing adopted. **E029 (H1, research-only): a CUSUM calibration-drift detector cries wolf on the family's own development record** (0.716 alarms per 1,000 h vs 0.196 for a calibrated control) because the family's calibration wanders in multi-week runs ~3–4 times a year; it would catch a persistent 0.10 offset in ~a week, 0.05 in ~two weeks. Not used, never wired. Context for the E026 watch item: a few weeks of live over-statement is ordinary for this family — only the registered 2,000/5,000 h checkpoints judge persistence. Every section of `research/INTERIM_PLAN_AUDIT.md` is now COMPLETE or CONTINUOUS; the remaining work before 500 h is the session loop. 473 unit tests.

## Research phase — rules and decisions (2026-09-19)

The user's research brief (from ChatGPT, reviewed and adopted) governs everything after Phase 1. Its order is binding: **fix → prove the fixes → evaluate what exists → improve only on evidence.** Decisions already made:

- **Time split** is defined in `research/EXPERIMENTS.md`. The **final holdout (2025-07-01 → 2026-08-19) is sealed** — never inspected, tuned on, or used to pick anything; evaluated once at the very end. The 30 days before go-live are a contaminated buffer (seen once). The live record from 2026-09-19 is the ultimate test.
- **Primary horizon is NOT decided.** Evaluate 1h, 6h, 24h (and 72h, 168h) with one consistent framework; choose on evidence.
- **Targets:** both binary direction (UP/DOWN) and three-class (UP/NEUTRAL/DOWN) with an explicit, configurable, documented definition (reference = close at cutoff; outcome = close of the candle opening at as_of+H; threshold fixed-% vs volatility-scaled to be tested, not assumed).
- **Free data only.** Anything paid, revised-after-the-fact, or without trustworthy historical timestamps is UNAVAILABLE/UNSAFE — never fabricated. Historical news is UNAVAILABLE (no archive; an LLM's training knowledge would leak).
- **Feature-group order** (after the baseline is evaluated, one at a time): volatility → regime → derivatives → macro → on-chain → news → microstructure → social; data quality overrides the order.
- **Experiment log:** `research/EXPERIMENTS.md` + one JSON per experiment in `research/experiments/`. Exploratory vs confirmatory is always stated.
- **Do not change** scoring formulas, weights, thresholds, or prompts until the corrected baseline (pipeline 0.2.0, scoring 0.1.0) has been evaluated against simple baselines with uncertainty, per year and per regime. Scoring 0.1.0 is the thing under test.
- **Confidence** stays a labelled heuristic until calibrated on validation data (Brier, log loss, reliability buckets with intervals) — never presented as a probability before then.
- **Overlapping outcome windows** (hourly predictions, multi-hour horizons) mean n hours ≠ n independent trials: use block bootstrap for uncertainty and an embargo ≥ horizon at any train/validation boundary once anything is fitted.
- The AI's roles stay separated (news → structured score; explanation after the decision) and must themselves be tested (schema reliability, faithfulness of explanations, whether news adds information beyond simpler alternatives).

**Research state (2026-09-20, E000–E009; details in `research/ROADMAP.md`, `research/EXPERIMENTS.md`):** Phases 1–5, 7, 8, 8A complete. 47 candidate features tested fit-free across volatility, regime, derivatives, macro, on-chain and microstructure: **no directional feature is usable**; a consistent but tiny 1–6h *reversal* family exists (momentum, volume, taker-buy share; |ρ| ≤ 0.05); **move size is predictable** (recent volatility ρ up to 0.45, trade intensity up to 0.30, plus a time-of-day effect). Live watch list for confirmatory re-tests: funding rate (24h contrarian, held 2019–23), dollar/yield 5-day changes (168h negative). AI components verified (E009): news scorer reliable with a small order-sensitivity bias; explainer faithful and provably decision-neutral. **Phase 6 done (E010: walk-forward bench verified, 5/5 checks).** **Phase 9 experiments done, report pending:** E011 (direction, fitted logistic, 15 walk-forward runs) — no pass anywhere; no combinable directional signal exists in the free data. E012 (move size, fitted logistic) — **pass at 1h** (Brier −15%/−9% vs base rate, ρ 0.45/0.36 with realised move size, both periods), 6h near-miss, 24h no. The deliverable is therefore calibrated *uncertainty* (how big the next hour's move is likely to be) plus HOLD-heavy signals; direction stays ≈ base rate. **2026-09-21 (evening) — backend hardening A–M, security, performance and the readiness gate done in one pass** (`research/ROADMAP.md` → "Backend hardening roadmap", `docs/research/readiness_gate.md`): live shadow record of the move-size model running from GitHub (own table `shadow_move_size`, frozen artefact `move_size_1h_v1`), parity 44/44, data layer hardened (schema checks, staleness rule, bounded calls), failure modes tested and documented, idempotency proven on real Postgres, database invariants + append-only triggers applied live, reproducibility demonstrated, versioning guards (golden scoring pin, feature fingerprint, artefact hash, schema version 3), drift monitor, LLM robustness tests, security audit, requirements pinned. **Open user actions:** healthchecks.io heartbeat; least-privilege DB role; PAT renewal 2027-09-20. **Holdout: sealed.** **INCIDENT 2026-09-21/22, fixed 2026-09-22 13:0x UTC** (`docs/ops/incident_2026-09-21_shadow_step.md`): the hourly job failed on ~half of all runs for ~17 hours. Only the research shadow step failed — the **live record was never affected** (18/18 predictions in the window, no fallback data, no timestamp violations, parity 62/62); the shadow record lost 8 hours, which are **not** backfilled (a recomputed probability would not be prospective). Root cause: the Phase G feature guard hashed values to 12 digits, and GitHub runners differ in the last bits (a 1e-14 difference flips that hash 56% of the time). Fixed by comparing values with `rtol = 1e-6`; shadow failures are now recorded in `shadow_run_errors` instead of failing the job; the watchdog alarms on persistent shadow errors; the weekly report shows them; the prospective rule is now enforced in code. Readiness gate reopened and revised. **Lesson: a guard on meaning must never be a bit-exact comparison across machines, and a research add-on must never be able to fail the job that carries the live record.** **Second, unrelated defect found the same day while reading the weekly report:** the news scorer's `max_tokens` (2048) could not hold an answer for more than ~50 headlines, and the live 24h window grew from 16 to 63 items — so **17 predictions (2026-09-21 20:00 → 2026-09-22 12:00 UTC) were made with news unavailable**; those rows mark themselves (`completeness_score` 0.85, news weight 0, `ai_model_news` NULL, `run_meta.news_error`), so any analysis can find them. Fixed (16384, prompt/schema unchanged so E009 still applies). **The test suite now runs on GitHub's runners on every push** (`.github/workflows/tests.yml`). **Cost note:** AI cost scales with news volume — at ~60 headlines/hour it is roughly $0.02/run (~$15/month), above the original "well under $10/month" estimate; halving it would mean dropping the echoed headline from the news schema, which is a change to the AI's task and needs an E009 re-run, so it awaits the user's decision. Earlier the same day: Phase 9 gated and reported; **Phase 10 closed without optimisation** (nothing to re-weight towards — adjustment 6); **Phase 11 done (E013): Platt scaling chosen by the pre-registered rule**, validation ECE 0.014, ≤ 0.03 every year; **Phase 12 prepared, NOT run** (E014 pre-registered, script dry-run on validation; unsealing needs the user's explicit go-ahead — `research/HOLDOUT_ACCESS.log` must not exist until then); **Phase 13 weekly report built**, first report `research/monitoring/weekly_2026-09-21.md`. The live outcome tracker stores returns as FRACTIONS (0.0044 = +0.44%) despite the column name `pct_change_from_prediction`.

**2026-09-22 (late) — scoring 0.1.0 → 0.2.0, on the user's decision.** A flaw found by reading the live scoring path: "enough history" counted ROWS, so a window containing missing hours was treated as consecutive (a 200-hour average could span 233 hours; the volume category compared the close with whatever candle sat six ROWS back, up to 33 hours off; "the last 20 hours" of trend structure could be 40). **9.26% of replayed hours since 2017 were affected.** Fixed: indicators run on the longest unbroken hourly run ending at the reference candle, the volume reference hour is found by timestamp, trend structure needs 20 real hours, and anything whose hours are missing is **unavailable** rather than wrong; `IndicatorSet` now records `window_hours`/`consecutive_hours`. Formulas, weights and thresholds untouched. **Verified before deploying (E016):** clean hours (62,262) bit-identical, gap-affected hours lose categories (completeness 0.850 → 0.568, 42.5% of signals change), 3.94% of the whole record. **Baseline re-run (E017): conclusion unchanged — no evidence at any horizon**; validation figures identical to E001 (no gap-affected hours there), exploration edges all shrank, and the only interval that had excluded zero (6h exploration) now includes it. Live since the 16:12 UTC run. **Precision work the same day:** pandas' default CSV float parser is not bit-exact (loses ~7e-12); all research loaders now pass `float_precision="round_trip"`, guarded by `tests/test_numeric_precision.py`, and an E016 claim that blamed "CSV precision" was corrected — the true difference was zero. Bootstrap endpoints measured: at 500 resamples they move ±0.03 pp between seeds, so the weekly report now uses 2,000 and states that endpoints are good to ~0.02 pp. Every reported accuracy carries a Wilson interval. **E015 (new signals, fit-free, pre-registered): 0 of 80 tests pass**; the strongest directional consistency the programme has found (`up_streak_6` at 1h, ρ −0.080/−0.060, monotone across 12 streak levels) earns 0.4× the cost of acting — real structure, no usable signal.

## Operating rules from 2026-09-21 (master operating prompt — binding)

- **The final holdout stays SEALED** until the backend readiness gate is met and a final no-access audit passes; opening it is a user decision, never an assumption. `research/HOLDOUT_ACCESS.log` must not exist. Guards: `load_bars` truncates at the holdout unless `allow_holdout=True` with a written reason (logged); `build_frame` only takes holdout candles through that route.
- **Phase 13 is the current stage**, followed by the backend hardening roadmap A–M, security, performance, and the readiness gate — all listed with their meaning in `research/ROADMAP.md` → "Backend hardening roadmap". Do not skip stages; document any reorder before acting.
- **Live move-size shadow record**: the E012 + Platt 1h model runs alongside the live job with its own model version and table; it never changes the BUY/HOLD/SELL decision, never trades, and is judged only after outcomes exist.
- Reports in simple English: WHAT HAPPENED → WHY IT MATTERS → WHAT CHANGED → TESTS → RESULT → PROBLEMS → CURRENT STATE → NEXT; the fixed PHASE COMPLETED block for finished phases; important problems reported immediately.
- "Backend done" is only claimed after the strict readiness gate; it never means "predicts the market". Frontend work later never freezes the backend, and never bends research correctness to a UI need.

## How to run things

All commands from the project folder, using the virtual environment (`.venv\Scripts\python.exe` on Windows):

- `python run.py` — one full analysis, saved to the database (exits early if this hour is already saved). `--no-save` to skip saving.
- `python -m agent.outcome_tracker` — grade past predictions that are old enough.
- `python -m agent.healthcheck --max-age-hours 2` — exit 1 if the newest prediction is stale (used by the workflows).
- `python -m agent.backtest.runner --days 30` — technical-only backtest; CSV lands in `backtests/`. Never run it over the sealed holdout.
- `python -m agent.research.weekly_report` — the Phase 13 weekly live report (reads the database, small Binance fetch for the paper record; `--no-paper` skips that). Output in `research/monitoring/`. Never writes to live tables.
- `python -m agent.research.holdout_eval --dry-run` — proves the Phase 12 script on the validation period. **`--unseal` opens the sealed holdout once and forever — only on the user's explicit go-ahead.**
- `python -m pytest tests/` — run the tests (no network or database needed).
- `BITCOIN_AGENT_DB_TESTS=1 python -m pytest tests/integration -q` — tests against real Postgres in a scratch schema it creates and drops (skipped by default): idempotency, the public-API lockdown layer by layer, the least-privilege role (`docs/ops/least_privilege_role.sql`, run as a throwaway role), and the **drift check** — the live database must match what the repository builds. **Run it at every weekly audit** (it is too heavy for the 3-hourly watchdog: it creates and drops a schema), and after any change made in the Supabase dashboard.
- `python -m agent.migrate` — **the only way to change the schema**: applies all three schema files (`agent/database`, `agent/shadow`, `agent/api`) in one transaction, then prints the schema version and the public-API posture. Safe to re-run. For a migration that raises `SCHEMA_VERSION`, run it against the database **before** deploying the code (the job accepts a database ahead of it, never behind). The hourly job never runs schema DDL.
- `python -m agent.database.security` — exit 1 if any table is reachable through Supabase's public API (the watchdog runs it every 3h).
- `python -m agent.database.role_check [--connected]` — the least-privilege role against `docs/ops/least_privilege_role.sql`; `--connected` also requires this connection to BE `bitcoin_agent` (the watchdog runs it).
- `python tools/dependency_audit.py` — every installed package against OSV.dev; exit 1 on a known vulnerability. Part of the weekly audit.
- `python tools/guard_mutations.py [--only TEXT]` — breaks each guard on purpose and requires the tests to catch it (both controls first). Leaves the tree byte-identical; refuses to run on uncommitted target files.
