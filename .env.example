# Copy this file to .env and fill in real values for local testing.
# .env is git-ignored — it will never be committed. In GitHub Actions, these
# same names are set as encrypted repo Secrets instead of a file.

COINGECKO_API_KEY=
ANTHROPIC_API_KEY=
DATABASE_URL=
HEARTBEAT_URL=
