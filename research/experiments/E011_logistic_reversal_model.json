{
  "id": "E011",
  "date": "2026-09-20",
  "git_commit": "(this commit)",
  "status": "exploratory (pre-registered criterion; one controlled model change)",
  "hypothesis": "A small L2-regularised logistic regression that combines the weak, individually consistent effects found in E002-E008 (short-horizon reversal in momentum, volume and taker-buy share; contrarian funding; dollar/yield 5-day changes; with volatility and trade intensity as conditioning inputs, plus the baseline category scores) produces a directional signal that is stable out-of-sample under walk-forward validation, where the fixed-weight scoring 0.1.0 does not.",
  "acceptance_criterion": "PRE-REGISTERED. On the concatenated out-of-sample predictions of the expanding walk-forward (purge = H, embargo 24h, quarterly test blocks, >= 365 days of training), the model 'adds a stable signal at horizon H' only if ALL hold in BOTH the exploration-OOS (test blocks before 2024) and validation-OOS (2024-01 -> 2025-06) periods: (1) acted-direction accuracy > the naive rate max(base, 1-base) on the same rows; (2) the edge (mean forward return when p > 0.5 minus when p < 0.5) has a block-bootstrap 95% interval excluding zero; (3) Brier score below the base-rate Brier. Three horizons are tested as three fixed, separately reported tests (1h, 6h, 24h) -- not a search; a pass at one horizon is a finding for that horizon only. The rolling layout is a robustness check, reported, never used to choose. TRIPWIRE: acted accuracy > 0.60 or Brier < 0.24 at 24h is treated as suspicious and investigated before being recorded.",
  "pipeline_version": "0.2.0",
  "scoring_version": "0.1.0 (untouched; baseline evaluated on identical test rows)",
  "model": {
    "type": "logistic regression, L2 penalty, C = 0.1 (fixed in advance, no search)",
    "preprocessing": "StandardScaler fitted on each fold's training rows only",
    "library": "scikit-learn 1.9.1"
  },
  "feature_sets": {
    "A_candle_only (full range from 2017-08)": [
      "trend_score",
      "momentum_score",
      "volume_score",
      "chart_pattern_score",
      "tr_mean_14_rel",
      "trades_rel_168h",
      "taker_buy_share_1h",
      "taker_buy_share_6h",
      "vol_pct_720"
    ],
    "B_all_sources (from 2019-12, when funding/premium exist)": [
      "trend_score",
      "momentum_score",
      "volume_score",
      "chart_pattern_score",
      "tr_mean_14_rel",
      "trades_rel_168h",
      "taker_buy_share_1h",
      "taker_buy_share_6h",
      "vol_pct_720",
      "funding_last",
      "premium_mean_24",
      "dxy_ret_5d",
      "tnx_chg_5d"
    ],
    "A_minus_volpct (supplementary coverage check, declared after the first run, before it ran)": [
      "trend_score",
      "momentum_score",
      "volume_score",
      "chart_pattern_score",
      "tr_mean_14_rel",
      "trades_rel_168h",
      "taker_buy_share_1h",
      "taker_buy_share_6h"
    ]
  },
  "dataset": {
    "source": "E001 replay + research feature snapshots (2026-09-19/20)",
    "snapshot": "as recorded per group",
    "range": "2017-08-27 -> 2025-06-30 (holdout excluded); set B effectively 2019-12 ->"
  },
  "periods": {
    "exploration": "OOS test blocks before 2024-01-01",
    "validation": "OOS test blocks 2024-01-01 -> 2025-06-30",
    "calibration": "none in this experiment (Phase 11)",
    "holdout": "sealed, not read"
  },
  "horizons_hours": [
    1,
    6,
    24
  ],
  "target": {
    "definition": "binary direction (UP if return > 0)",
    "reference": "close at cutoff",
    "outcome": "close of candle opening at as_of+H"
  },
  "validation_design": "expanding walk-forward, min_train 365d, test blocks 90d, purge H, embargo 24h; rolling 730d as robustness",
  "hyperparameters": {
    "C": 0.1,
    "max_iter": 2000,
    "block_bootstrap": "block = max(48, 2H), 500 resamples"
  },
  "calibration_method": null,
  "metrics": {
    "runs": 15,
    "passes": 0,
    "tripwire_fired": false,
    "1h_all_layouts": {
      "acc_minus_naive_pp": "+1.5 to +2.5 in both periods",
      "brier_gain": "0.0009 (0.4%)",
      "return_edge_pct": "≈0 (A exp: -0.010 [-0.026,+0.006] / +0.009 [-0.009,+0.030])",
      "top_coefs": "taker_buy_share_1h and momentum_score negative in 100% of folds"
    },
    "6h": "accuracy +0.1 to +0.8 pp; Brier mixed; edge CI includes 0 (A- exploration edge negative, CI excludes 0)",
    "24h": "accuracy within ±0.5 pp of naive; set B Brier worse than base rate in exploration (0.258 vs 0.250)",
    "coverage_check_A_minus": "same conclusion; rows 56,309 vs 45,740",
    "by_move_size_1h_A_expanding": {
      "smallest_25%": "0.512 vs 0.500",
      "25-50%": "0.534 vs 0.507",
      "50-75%": "0.552 vs 0.514",
      "75-90%": "0.521 vs 0.514",
      "largest_10%": "0.465 vs 0.509"
    }
  },
  "results": "NO PASS at 1h, 6h or 24h in any of 15 runs. See research/results/E011/summary.md. The only stable pattern is the known 1-6h reversal family, ~+2 points of direction accuracy at 1h earned on small moves and lost on the largest ones, so the return edge is zero.",
  "consistent_across_time": "The 1h accuracy excess is consistent (7-8 of 8 years, both periods, five layouts) but return-neutral everywhere; nothing else is consistent.",
  "leakage_check": "Bench verified in E010; fold gaps 26h (1h), 31h (6h), 49h (24h) = H + 24 + 1 on every fold except where a data hole widens them; results are at chance in return terms, the opposite of a leak signature.",
  "overfitting_suspected": "Set B at 24h/6h: Brier worse than the base rate in exploration with 19 folds and 13 inputs -- classic overfitting of a signal-free target; noted, not tuned away.",
  "affected_design": "Phase 10 not justified (nothing to re-weight towards). Directional output stays P(up) ≈ base rate -> HOLD-heavy. Research proceeds to the uncertainty (move-size) model E012 and its calibration.",
  "notes": "Six runs total (2 feature sets x 3 horizons) on the primary layout, plus rolling robustness; all reported. Nothing about the live system changes regardless of outcome.",
  "coverage_note_written_before_remaining_runs": "First primary run (set A, 1h) showed 45,740 usable rows instead of 59,775: vol_pct_720 needs 720 complete hours and the no-fill rule blanks it for 30 days after each of the 28 candle gaps (2018-2021), removing ~24% of exploration hours in post-gap months. The pre-registered lists stay as they are for the verdict. ONE labelled supplementary check is added and declared here before it is run: set A without vol_pct_720 ('A_minus_volpct'), expanding, same horizons -- to show whether the conclusion depends on the dropped months. It is a robustness check, never used to choose."
}